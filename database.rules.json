{
  "rules": {
    "markers": {
      ".read": true,
      ".write": "auth != null",
      "$markerId": {
        "note": {
          ".write": "newData.isString() && newData.val().length < 500"
        }
      }
    }
  }
}
